# Application bindings
bindd = SUPER, RETURN, Terminal, exec, uwsm-app -- xdg-terminal-exec --dir="$(omarchy-cmd-terminal-cwd)"
bindd = SUPER ALT, RETURN, Tmux, exec, uwsm-app -- xdg-terminal-exec --dir="$(omarchy-cmd-terminal-cwd)" tmux new
bindd = SUPER SHIFT, RETURN, Browser, exec, omarchy-launch-browser
bindd = SUPER SHIFT, F, File manager, exec, uwsm-app -- nautilus --new-window
bindd = SUPER ALT SHIFT, F, File manager (cwd), exec, uwsm-app -- nautilus --new-window "$(omarchy-cmd-terminal-cwd)"
bindd = SUPER SHIFT, B, Browser, exec, omarchy-launch-browser
bindd = SUPER SHIFT ALT, B, Browser (private), exec, omarchy-launch-browser --private
bindd = SUPER SHIFT, M, Music, exec, omarchy-launch-or-focus spotify
bindd = SUPER SHIFT, N, Editor, exec, omarchy-launch-editor
# bindd = SUPER SHIFT, D, Docker, exec, omarchy-launch-tui lazydocker
bindd = SUPER SHIFT, G, Signal, exec, omarchy-launch-or-focus ^signal$ "uwsm-app -- signal-desktop"
bindd = SUPER SHIFT, O, Obsidian, exec, omarchy-launch-or-focus ^obsidian$ "uwsm-app -- obsidian -disable-gpu --enable-wayland-ime"
bindd = SUPER SHIFT, W, Typora, exec, uwsm-app -- typora --enable-wayland-ime
bindd = SUPER SHIFT, SLASH, Passwords, exec, uwsm-app -- 1password

# If your web app url contains #, type it as ## to prevent hyprland treating it as a comment
bindd = SUPER SHIFT, C, Calendar, exec, omarchy-launch-webapp "https://calendar.google.com/calendar/"
bindd = SUPER SHIFT CTRL, G, Google Messages, exec, omarchy-launch-or-focus-webapp "Google Messages" "https://messages.google.com/web/conversations"
bindd = SUPER SHIFT, P, Google Photos, exec, omarchy-launch-or-focus-webapp "Google Photos" "https://photos.google.com/"

# Add extra bindings

# Overwrite existing bindings, like putting Omarchy Menu on Super + Space

# capture screen
bindd = ALT SHIFT, S, Screenshot (Area), exec, grim -g "$(slurp)" - | wl-copy
# My custom key "ESC" for 68% keyboard
bind = SHIFT, Escape, exec, wtype "~"

# ============================================================
# CUSTOM KEYBINDINGS (HJKL NAVIGATION & FIXES)
# ============================================================

# 1. GIẢI PHÓNG CÁC PHÍM ĐANG XUNG ĐỘT
unbind = SUPER, K      # Gỡ phím K (đang mở cheatsheet)
unbind = SUPER, I      # Gỡ phím I (nếu có bind cũ)
unbind = SUPER, slash  # Gỡ phím / (đang upscale màn hình)
unbind = SUPER, L
unbind = SUPER, J
# 2. GÁN LẠI CHEATSHEET SANG SUPER + I (Information)
bindd = SUPER, I, Show keybindings, exec, omarchy-cheatsheet

# 3. DI CHUYỂN TIÊU ĐIỂM (FOCUS) BẰNG SUPER + HJKL
# bindd = SUPER, H, Focus left, movefocus, l
# bindd = SUPER, J, Focus down, movefocus, d
# bindd = SUPER, K, Focus up, movefocus, u
# bindd = SUPER, L, Focus right, movefocus, r

bindd = SUPER, H, Move window focus left, movefocus, l
bindd = SUPER, L, Move window focus right, movefocus, r
bindd = SUPER, K, Move window focus up, movefocus, u
bindd = SUPER, J, Move window focus down, movefocus, d

# Gán phím tắt mới cho việc Toggle màn hình
bindd = SUPER SHIFT, D, Toggle Laptop Monitor, exec, ~/.local/bin/toggle_laptop.sh
